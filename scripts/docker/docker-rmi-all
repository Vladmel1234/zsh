#!/usr/bin/env bash

# Description  : Silently remove all images.
# Usage        : docker rmi-all
# Dependencies :
# Example      :
# Notes        :
# Source       :

docker kill $(docker ps -a -q) 2>&1
docker rm $(docker ps -a -q) 2>&1
docker rmi $(docker ps -a -q) 2>&1